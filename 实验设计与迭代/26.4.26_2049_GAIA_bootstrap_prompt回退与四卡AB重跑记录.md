# GAIA bootstrap prompt 回退与四卡 AB 重跑记录

时间：2026-04-26 20:49

## 结论

本次将 `prompts/bootstrap_skill_system.md` 回退到 commit `36037b2` 的 4/21 版本。

回退原因：
- 4/21 bootv3 bootstrap test：`14/82`。
- 4/26 本地 Qwen3 API 对齐后，用 4/21 bootstrap skill 补跑 test：`13/79` partial。
- 4/26 重新 fresh 生成 bootstrap skill 后，bootstrap test：`0/82`。

差异集中在 bootstrap 生成出的 hard graph。4/26 fresh prompt 加了更强的 CONCLUDE gate 约束，新 skill 在 bootstrap dev 中 `70/83` 为空答案，bootstrap test 中 `72/82` 为空答案。这个表现不符合旧版 bootstrap 的稳定水平。

## 回退内容

回退后的 prompt 关键规则：
- `Make CONCLUDE reachable quickly from any phase that can produce a final candidate answer.`

移除的高风险约束：
- 将 `CONCLUDE` 强设为严格 gated finalization。
- 对 evidence/verify 阶段直接到 `CONCLUDE` 的过强限制。
- `INIT -> CONCLUDE` 及各阶段收口条件的过窄描述。

## 本轮重跑拓扑

不重跑 direct。

第一阶段：
- `bootstrap dev`：GPU0 / `http://127.0.0.1:8100/v1` / c20。
- `bootstrap test`：GPU2 / `http://127.0.0.1:8102/v1` / c20。

第二阶段：
- bootstrap dev/test 完整结束后，用完整 dev states 生成 A/B skill。
- A/B 生成使用回退 prompt 生成出的 bootstrap skill 与完整 dev state。

第三阶段：
- `A_full dev`：GPU0 / c20。
- `A_full test`：GPU1 / c20。
- `B_sharded dev`：GPU2 / c20。
- `B_sharded test`：GPU3 / c20。

队列结束后清理本轮 Qwen3 vLLM 服务，释放显存。

## 启动记录

run prefix：`20260426_205027_rollback_prompt`

队列进程：
- PID：`38057`
- 状态：运行中。
- 日志：`/data/xsy/project_gaia_skillrl/runs/_launch_logs/20260426_205027_rollback_prompt_bootstrap_ab_4gpu_c20.log`
- 命令：

```bash
env GAIA_FRESH_QUEUE_PREFIX=20260426_205027_rollback_prompt GAIA_FRESH_QUEUE_CONCURRENCY=20 GAIA_FRESH_QUEUE_TAIL_THRESHOLD=3 GAIA_QUEUE_CLEANUP_SERVER_PIDS=3838719,36651,36723,36798 /data/xsy/project_gaia_skillrl/.venv/bin/python /data/xsy/project_gaia_skillrl/scripts/run_gaia_fresh_bootstrap_ab_tail_queue.py
```

本轮本地 Qwen3 vLLM 服务：
- GPU0 / port 8100 / PID `3838719`：沿用已运行服务。
- GPU1 / port 8101 / PID `36651`：`/data/xsy/project_gaia_skillrl/runs/_launch_logs/qwen3_8b_local_vllm_gpu1_p8101_20260426_204924.log`
- GPU2 / port 8102 / PID `36723`：`/data/xsy/project_gaia_skillrl/runs/_launch_logs/qwen3_8b_local_vllm_gpu2_p8102_20260426_204924.log`
- GPU3 / port 8103 / PID `36798`：`/data/xsy/project_gaia_skillrl/runs/_launch_logs/qwen3_8b_local_vllm_gpu3_p8103_20260426_204924.log`

队列脚本会在全链路结束后停止 `GAIA_QUEUE_CLEANUP_SERVER_PIDS` 中的四个服务，并更新 `/data/xsy/活的进程.csv`。

已启动子任务：
- `bootstrap dev`：PID `38124`，run `20260426_205027_rollback_prompt_fresh_bootv3_boot_dev_c20`，日志 `/data/xsy/project_gaia_skillrl/runs/_launch_logs/20260426_205027_rollback_prompt_fresh_bootv3_boot_dev_c20_20260426_205028.log`。
- `bootstrap test`：PID `38253`，run `20260426_205027_rollback_prompt_fresh_bootv3_boot_test_c20`，日志 `/data/xsy/project_gaia_skillrl/runs/_launch_logs/20260426_205027_rollback_prompt_fresh_bootv3_boot_test_c20_20260426_205131.log`。

20:51 状态：
- bootstrap skill 已生成：`/data/xsy/project_gaia_skillrl/runs/2026/4/2026-4-26/20260426_205027_rollback_prompt_fresh_bootv3_boot_dev_c20/bootstrap/skill_after_bootstrap/gaia-general-skill/SKILL.md`
- `bootstrap dev` 与 `bootstrap test` 已进入 final validation。
